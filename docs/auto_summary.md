# ML Pipeline Summary

_Generated by auto_pipeline.py on 2026-05-26 07:59:41_

---

## 1. Dataset Overview

| Property | Value |
|---|---|
| File | sample.csv |
| Rows | 60 |
| Columns | 4 |
| Target Column | `survived` |
| Task Type | **Classification** |
| Unique Target Values | 2 |
| Deployment Platform | ask_later |

### Missing Values
No missing values detected.

---

## 2. Features Used

**Numeric (3):** `age`, `salary`, `experience`

**Categorical (0):** _none_

**Dropped (>50% missing):** _none_

---

## 3. Preprocessing Pipeline

| Step | Detail |
|---|---|
| Missing (numeric) | SimpleImputer(strategy="median") |
| Missing (categorical) | SimpleImputer(strategy="most_frequent") |
| Scaling | StandardScaler |
| Encoding | OneHotEncoder(handle_unknown="ignore") |
| Train/Test Split | 80/20, stratified, random_state=42 |
| Train size | 48 rows |
| Test size | 12 rows |

---

## 4. Model Selection & Hyperparameter Tuning

GridSearchCV(cv=3, n_jobs=-1, scoring="accuracy")

| Model | CV Score | Best Params |
|---|---|---|
| RandomForest | 0.9792 | {'max_depth': None, 'n_estimators': 100} |
| LogisticRegression | 0.9167 | {'C': 1} |
| GradientBoosting | 0.8958 | {'learning_rate': 0.05, 'n_estimators': 100} |

**Winner:** `RandomForest` with CV accuracy = **0.9792**

---

## 5. Model Evaluation

| Metric | Value |
|---|---|
| Test Accuracy | 0.8333 |
| CV Score (accuracy) | 0.9792 |

**Classification Report:**
```
              precision    recall  f1-score   support

           0       0.88      0.88      0.88         8
           1       0.75      0.75      0.75         4

    accuracy                           0.83        12
   macro avg       0.81      0.81      0.81        12
weighted avg       0.83      0.83      0.83        12

```

---

## 6. Final Pipeline Architecture

```
CSV Input
    └── ColumnTransformer
            ├── numeric  → SimpleImputer(median) → StandardScaler
            └── categorical → SimpleImputer(most_frequent) → OneHotEncoder
                └── RandomForest(max_depth=None, n_estimators=100)
                        └── Prediction
```

---

## 7. Artifacts

| File | Description |
|---|---|
| models/final_pipeline.pkl | Trained sklearn Pipeline (preprocessor + model) |
| models/label_encoder.pkl | Fitted LabelEncoder for target classes |
| plots/eda_correlation.png | Feature correlation heatmap |
| plots/eda_target.png | Target variable distribution |


---

## 8. Reproducibility

```python
import joblib, pandas as pd

pipeline = joblib.load("models/final_pipeline.pkl")
df = pd.read_csv("data/sample.csv")
X = df.drop(columns=["survived"])
predictions = pipeline.predict(X)
print(predictions)
```
